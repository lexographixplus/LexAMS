import { useEffect, useState } from 'react';
import { useParams } from 'react-router-dom';
import PublicExperienceLayout, { PublicCard, PublicNotice } from '../../components/PublicExperienceLayout';
import { publicApi } from '../../lib/publicApi';
function sessionFor(a){const t=new Date();t.setHours(0,0,0,0);const s=new Date(a.start_date+'T00:00:00');const d=Math.floor((t-s)/86400000);return `Day ${Math.max(1,Math.min(d+1,Number(a.sessions||1)))}`}
function fmtRange(a){const s=new Date(a.start_date+'T00:00:00'),e=new Date(a.end_date+'T00:00:00');return a.start_date===a.end_date?s.toLocaleDateString('en-GB',{day:'numeric',month:'short',year:'numeric'}):`${s.toLocaleDateString('en-GB',{day:'numeric',month:'short'})} – ${e.toLocaleDateString('en-GB',{day:'numeric',month:'short',year:'numeric'})}`}
export default function AttendancePublic(){const {token}=useParams();const [activity,setActivity]=useState(null),[email,setEmail]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(true),[done,setDone]=useState(null),[submitting,setSubmitting]=useState(false);useEffect(()=>{publicApi('attendance',token).then(b=>setActivity(b.activity)).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[token]);async function submit(e){e.preventDefault();setSubmitting(true);setError('');try{const b=await publicApi('attendance',token,{method:'POST',body:JSON.stringify({email,sessionLabel:sessionFor(activity)})});setDone(b.name)}catch(e){setError(e.message)}finally{setSubmitting(false)}}if(loading)return <PublicExperienceLayout eyebrow="Attendance" title="Loading check-in…" narrow/>;if(!activity)return <PublicExperienceLayout eyebrow="Attendance" title="Check-in unavailable" narrow><PublicNotice tone="error">{error}</PublicNotice></PublicExperienceLayout>;const session=sessionFor(activity);return <PublicExperienceLayout eyebrow="Attendance check-in" title={activity.title} description={`${fmtRange(activity)}${activity.venue?` · ${activity.venue}`:''}`} organizationName={activity.organization_name} organizationLogo={activity.organization_logo} narrow><PublicCard>{done?<div style={{textAlign:'center'}}><div style={{fontSize:38}}>✓</div><h3>Attendance recorded</h3><p style={{color:'#687587'}}>{done} · {session}</p></div>:<form onSubmit={submit}><h3>{session}</h3><p style={{color:'#687587'}}>Confirm your attendance using the email address used for registration.</p><label className="lex-public-label">Registered email</label><input className="lex-public-input" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@example.org"/>{error&&<PublicNotice tone="error">{error}</PublicNotice>}<div className="lex-public-actions"><button className="lex-public-button" disabled={submitting}>{submitting?'Recording…':'Check in'}</button></div></form>}</PublicCard></PublicExperienceLayout>}
