import { useState } from 'react';
import { Link, Navigate } from 'react-router-dom';
import { useAuth } from '../contexts/AuthContext';
import AuthPageLayout from '../components/AuthPageLayout';
export default function Signup(){const {signUp,user,loading}=useAuth();const [fullName,setFullName]=useState(''),[orgName,setOrgName]=useState(''),[email,setEmail]=useState(''),[error,setError]=useState(null),[submitting,setSubmitting]=useState(false),[emailSent,setEmailSent]=useState(false);if(user&&!loading)return <Navigate to="/app" replace/>;async function submit(e){e.preventDefault();setError(null);setSubmitting(true);const {error:err}=await signUp(email,null,fullName,orgName);if(err)setError(err.message||'Could not start your account.');else setEmailSent(true);setSubmitting(false)}return <AuthPageLayout eyebrow="Start free" title={emailSent?'Check your inbox':'Create your workspace'} description={emailSent?'Use the secure setup link to finish creating your workspace.':'Start on Free with up to 50 participants. No card is required.'} footer={<>Already have an account? <Link to="/login">Sign in</Link></>}>{emailSent?<div style={{textAlign:'center'}}><div style={{fontSize:36}}>✉</div><p className="authx-desc">We sent a secure setup link to <strong>{email}</strong>.</p><p className="authx-note">Open it to finish creating your LexAMS workspace.</p></div>:<form onSubmit={submit}><div className="authx-fields"><div><label className="authx-label">Full name</label><input className="authx-field" required value={fullName} onChange={e=>setFullName(e.target.value)} placeholder="Your full name"/></div><div><label className="authx-label">Organisation name</label><input className="authx-field" required value={orgName} onChange={e=>setOrgName(e.target.value)} placeholder="e.g. Horizon Community Foundation"/></div><div><label className="authx-label">Email address</label><input className="authx-field" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@example.org" autoComplete="email"/></div></div>{error&&<div className="authx-error">{error}</div>}<button className="authx-btn" disabled={submitting}>{submitting?'Sending setup link…':'Create free workspace'}</button><p className="authx-note">Free includes up to 50 participants. Upgrade only when you need more capacity or Pro features.</p></form>}</AuthPageLayout>}
